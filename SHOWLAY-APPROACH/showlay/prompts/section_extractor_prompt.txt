You are extracting canonical rows from one section of a healthcare PDF form template.
Use the Question Type taxonomy and Excel/output field meanings below as the source of truth for classification and columns.
You also receive an extraction policy from the previous model. That policy is not a final answer and not a taxonomy; it is PDF-specific guidance about where fields, labels, options, widgets, branches, and ignoreable page chrome appear.

Question Type taxonomy:
$question_type_taxonomy

Excel/output field meanings:
$excel_field_taxonomy

Canonical Question Type values:
$canonical_question_types

Canonical Excel columns:
$canonical_excel_columns

Extractor-only support fields:
- page: PDF page number for this row.
- bbox: bounding box for the visible row/prompt when confidently known.
- source_ids: text/widget source ids used as evidence.
- external_id: printed item/code when a source code clearly identifies the row.
- branching_source: visible skip/display/conditional wording from the PDF.
- confidence_hint: confidence for this extracted row.
- warnings: short notes when the row needs review.

Generic extraction rules:
- Visible rendered text and spatial layout are primary.
- AcroForm widgets are supporting evidence only: use them for input affordance, type hints, bbox, and source_ids.
- Do not treat widget names like Text2 or undefined_3 as question text.
- Blank widget values are not answer values.
- Do not invent rows, question types, columns, answer options, branching, required markers, or source ids.
- Do not use question types outside the canonical Question Type values.
- Use the previous model's extraction policy to know what field patterns to expect in this PDF: label positions, blanks, widgets, option layout, repeated rows, tables, and branch text.
- For choice controls, classify using the taxonomy above, then use the policy to locate the shared question text and printed option labels.
- Treat Answer Text as printed option labels only for choice-like fields; do not put filled values or checked state there.
- For compact same-line fields, split each label/blank/widget into its own row when visible evidence shows separate inputs.
- If the previous policy conflicts with visible evidence in this section, follow visible evidence and add a warning.
- Return only via the provided JSON schema.

Document context:
- Document title: $document_title
- Document version: $document_version
- All sections: $all_sections
- Shared legends: $shared_legends
- This section: $section_name, pages $section_pages
- Current page chunk $chunk_number of $chunk_count: $page_numbers

Previous model input: extraction policy / PDF field-observation notes:
$extraction_policy

Section evidence:
- Text blocks: $text_blocks
- Widgets: $widgets
